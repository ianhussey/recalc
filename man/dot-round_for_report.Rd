% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/recalc_independent_t.R
\name{.round_for_report}
\alias{.round_for_report}
\title{Round a multiverse bound to the reporting precision.}
\usage{
.round_for_report(x, digits, rounding)
}
\description{
The reported value (p or d) has already been rounded by the original
analyst to \code{digits} decimal places under some rounding convention. For
the \code{inbounds} comparison to be apples-to-apples, the multiverse's
\link{min, max} must be rounded to the same precision under the same
convention; otherwise reported values just outside the unrounded
multiverse but within rounding tolerance produce spurious \code{FALSE}s
(most visible when the analytic value is within half a ULP of a
representational boundary such as p ≈ 0 or p ≈ 1).
}
\details{
For \code{"either"}, \code{"half_up"}, and \code{"bankers"} the closed preimage
interval is the symmetric ±½-ULP, so half-up is used as the canonical
rounding for the bound. For \code{"truncate"} the preimage is asymmetric
and we use \code{round_trunc} (toward zero) to match.
}
\keyword{internal}
