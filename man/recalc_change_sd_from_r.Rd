% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/prepost_r.R
\name{recalc_change_sd_from_r}
\alias{recalc_change_sd_from_r}
\title{Change-score SD expected for a given pre-post correlation (reverse direction)}
\usage{
recalc_change_sd_from_r(
  sd_pre,
  sd_post,
  r,
  sd_pre_digits = NULL,
  sd_post_digits = NULL,
  r_digits = NULL,
  sd_change = NULL,
  sd_change_digits = NULL,
  rounding = "either"
)
}
\arguments{
\item{sd_pre, sd_post}{Numeric. Reported pre-test and post-test SDs.}

\item{r}{Numeric. Pre-post correlation to assume.}

\item{sd_pre_digits, sd_post_digits}{Integer. Decimal places \code{sd_pre} /
\code{sd_post} were reported to. Required.}

\item{r_digits}{Integer. Decimal places \code{r} was reported to. Required.}

\item{sd_change}{Numeric. A reported change SD to check (optional).}

\item{sd_change_digits}{Integer. Required only when \code{sd_change} is given.}

\item{rounding}{One of \code{"either"}, \code{"half_up"}, \code{"bankers"},
\code{"truncate"}. See \code{\link{recalc_rounding}}.}
}
\value{
One-row tibble (see \code{\link{recalc_result}}).
}
\description{
\eqn{SD_{change} = \sqrt{SD_{pre}^2 + SD_{post}^2 - 2 r\, SD_{pre} SD_{post}}}.
The complement of \code{\link{recalc_prepost_r}}: given component SDs and a
correlation, what change SD should appear? Useful for checking a reported
change SD against the change SD a normal \eqn{r} (say 0.5) would produce.
}
\examples{
recalc_change_sd_from_r(sd_pre = 14.58, sd_post = 7.79, r = 0.46,
                        sd_pre_digits = 2, sd_post_digits = 2, r_digits = 2)
}
