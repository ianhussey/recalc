% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/regression_from_cor_mat.R
\name{recalc_regression_from_cor_mat}
\alias{recalc_regression_from_cor_mat}
\title{Recalculate a linear regression from a reported correlation matrix}
\usage{
recalc_regression_from_cor_mat(
  formula,
  cor_mat,
  n,
  sd_vector = NULL,
  reported_betas = NULL,
  reported_b = NULL,
  cor_mat_digits = NULL,
  n_digits = 0,
  sd_vector_digits = NULL,
  reported_betas_digits = NULL,
  reported_b_digits = NULL,
  level = 0.95,
  rounding = "either"
)
}
\arguments{
\item{formula}{A Wilkinson-notation regression formula, e.g. \code{y ~ x1 + x2}.
Variables must appear as row and column names of \code{cor_mat} (and of
\code{sd_vector} when supplied).}

\item{cor_mat}{Numeric. A symmetric correlation matrix with row and column
names. May contain extra variables not used in \code{formula}.}

\item{n}{Integer. Sample size.}

\item{sd_vector}{Optional named numeric vector of variable SDs. When
supplied, unstandardized regression coefficients and their CIs are also
recalculated. Must include all variables in \code{formula}.}

\item{reported_betas}{Optional named numeric vector of reported standardized
regression coefficients, one per predictor. When supplied, each value is
compared against the recalculated \code{Std.Beta} interval and \code{consistent} is
filled in for those rows.}

\item{reported_b}{Optional named numeric vector of reported unstandardized
regression coefficients, one per predictor. Compared against the
recalculated \code{b} interval. Only meaningful when \code{sd_vector} is supplied
(otherwise the function does not return \code{b} rows).}

\item{cor_mat_digits, n_digits, sd_vector_digits, reported_betas_digits, reported_b_digits}{Integer.
Number of decimal places each value was reported to. \code{cor_mat_digits} has
no default - the caller must state the precision of the reported
correlations. \code{sd_vector_digits}, \code{reported_betas_digits}, and
\code{reported_b_digits} are required only when their corresponding inputs
are provided.}

\item{level}{Numeric. CI level. Default 0.95.}

\item{rounding}{See \link{recalc_rounding}.}
}
\value{
A long-format tibble with one row per (predictor x quantity). The
\code{check} column names the identity recomputed (e.g. \code{"Std.Beta"},
\code{"Std.Beta CI lower"}, \code{"p"}, \code{"b"}); \code{predictor} is the predictor name
from \code{formula}. \code{reported}, \code{reported_lower}, \code{reported_upper}, and
\code{consistent} are populated for \code{Std.Beta} rows when \code{reported_betas} is
supplied, and for \code{b} rows when \code{reported_b} is supplied; \code{NA} otherwise.
}
\description{
Fits an OLS regression \code{Y ~ X1 + X2 + ...} directly from a reported
correlation matrix and sample size. Returns recalculated intervals on the
standardized regression coefficients, their Wald CIs, t statistics, and
p values. When a vector of SDs is supplied, unstandardized coefficients
\eqn{b_i = \beta_i \cdot \text{SD}_Y / \text{SD}_{X_i}} and their CIs are
also returned.
}
\details{
Identities used (OLS from a standardized covariance matrix):
\eqn{\beta = R_{xx}^{-1} r_{xy}};
\eqn{R^2 = \beta^\top r_{xy}};
\eqn{\text{SE}(\beta_i) = \sqrt{(1-R^2)/(N-k-1) \cdot [R_{xx}^{-1}]_{ii}}};
\eqn{t_i = \beta_i / \text{SE}(\beta_i)};
\eqn{\text{CI}_{1-\alpha} = \beta_i \pm t_{1-\alpha/2,\,N-k-1} \cdot \text{SE}(\beta_i)}.

Rounding intervals are propagated by enumerating
\eqn{2^{p(p-1)/2 + 1 + p_{\text{sd}}}} corner combinations, where \eqn{p} is
the number of variables in the model and \eqn{p_{\text{sd}}} is the number
of supplied SDs. Practical up to about \eqn{p = 6}.
}
\examples{
# Haller et al. (2022) correlation matrix and N
nm <- c("prosocial", "wellbeing", "support",
        "stress", "flexibility", "affect")
R <- matrix(c(1.00,  0.32,  0.34, -0.09,  0.21,  0.30,
              0.32,  1.00,  0.45, -0.54,  0.54,  0.64,
              0.34,  0.45,  1.00, -0.26,  0.32,  0.34,
             -0.09, -0.54, -0.26,  1.00, -0.52, -0.49,
              0.21,  0.54,  0.32, -0.52,  1.00,  0.50,
              0.30,  0.64,  0.34, -0.49,  0.50,  1.00),
            nrow = 6, dimnames = list(nm, nm))
recalc_regression_from_cor_mat(
  formula = prosocial ~ stress + affect + flexibility + support,
  cor_mat = R, n = 9496,
  cor_mat_digits = 2
)
}
