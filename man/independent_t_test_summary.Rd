% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/independent_t_test_summary.R
\name{independent_t_test_summary}
\alias{independent_t_test_summary}
\title{Multiverse of Independent Samples t-test Results (Summary Statistics)}
\usage{
independent_t_test_summary(
  m1 = NULL,
  m2 = NULL,
  sd1 = NULL,
  sd2 = NULL,
  n1 = NULL,
  n2 = NULL,
  m_digits = NULL,
  sd_digits = NULL,
  rounding = "either",
  p_operator = c("equals", "less_than", "greater_than", "less_than_or_equal_to",
    "greater_than_or_equal_to"),
  p = NULL,
  p_digits = NULL,
  p_methods = NULL,
  alpha = 0.05,
  d = NULL,
  d_ci_lower = NULL,
  d_ci_upper = NULL,
  d_digits = NULL,
  direction = c("m1_minus_m2", "m2_minus_m1", "both"),
  ci_methods = NULL,
  hedges_correction = NULL,
  include_se_sd_confusion = FALSE
)
}
\arguments{
\item{m1}{Numeric. Mean of Group 1.}

\item{m2}{Numeric. Mean of Group 2.}

\item{sd1}{Numeric. Standard deviation of Group 1 (assumed sample SD by default).}

\item{sd2}{Numeric. Standard deviation of Group 2 (assumed sample SD by default).}

\item{n1}{Numeric. Sample size of Group 1.}

\item{n2}{Numeric. Sample size of Group 2.}

\item{m_digits}{Integer. Number of decimal places to assume for the reported
means (\code{m1}, \code{m2}). This controls the width of the interval within
which the "true" unrounded mean is assumed to lie.}

\item{sd_digits}{Integer. Number of decimal places to assume for the reported
SDs (\code{sd1}, \code{sd2}). This controls the width of the interval within
which the "true" unrounded SD is assumed to lie.}

\item{rounding}{One of \code{"either"} (default), \code{"half_up"},
\code{"bankers"}, \code{"truncate"}. Selects how the reported \code{m1},
\code{m2}, \code{sd1}, \code{sd2} (and \code{p}, \code{d}, \code{d_ci_*}
when supplied) are mapped to their preimage rounding intervals. The
first three give the symmetric \eqn{\pm}\eqn{\frac{1}{2}}-ULP interval
(the standard half-up / bankers preimage is the same closed interval
modulo measure-zero tie handling); \code{"truncate"} gives the asymmetric
\eqn{[\text{value},\, \text{value} + \text{ULP}]} interval (signed for
negatives). See \link{recalc_rounding}.}

\item{p_operator}{Character. Defines the relationship between the reported
p-value and the actual calculated value. Use this to handle censored
values (e.g., "p < .001").
Options: \code{"equals"}, \code{"less_than"}, \code{"greater_than"},
\code{"less_than_or_equal_to"}, \code{"greater_than_or_equal_to"}.
Default is \code{"equals"}.}

\item{p}{Numeric. Reported p-value.}

\item{p_digits}{Integer. Number of decimal places to which the p-value was
reported. Must be supplied if \code{p} is non-\code{NULL}.}

\item{p_methods}{Character vector. P-value methods to include.
Allowed: \code{"student_t"}, \code{"welch_t"}.
If \code{NULL}, both are used.}

\item{alpha}{Numeric. Significance level for CIs (default is 0.05).}

\item{d}{Numeric. Reported Cohen's \emph{d} or Hedges' \emph{g} estimate.}

\item{d_ci_lower}{Numeric. Reported lower bound of the CI for \emph{d}.}

\item{d_ci_upper}{Numeric. Reported upper bound of the CI for \emph{d}.}

\item{d_digits}{Integer. Number of decimal places to which \emph{d} and its CI
bounds were reported. Must be supplied if \code{d} is non-\code{NULL}.}

\item{direction}{Character. Whether to compute the effect as M1 - M2,
M2 - M1, or both. One of \code{"m1_minus_m2"}, \code{"m2_minus_m1"},
\code{"both"}.}

\item{ci_methods}{Character vector. CI methods to include.
Allowed: \code{"wald_t"}, \code{"welch_t"}, \code{"nct"}.
If \code{NULL}, all are used.}

\item{hedges_correction}{Logical or NULL. If \code{TRUE}, the calculation applies
Hedges' correction factor (calculating Hedges' \emph{g}). If \code{FALSE}, it
calculates Cohen's \emph{d}. If \code{NULL} (default), the multiverse iterates
over both possibilities.}

\item{include_se_sd_confusion}{Logical. If \code{TRUE}, the multiverse also
includes a branch that interprets \code{sd1} and \code{sd2} as standard
errors and converts them to SDs via \eqn{SD = SE \times \sqrt{n}} before
computing effect sizes, CIs, and p-values. Default is \code{FALSE}.}
}
\value{
A list with three elements:
\itemize{
\item \code{reproduced}: Data frame summarising whether the reported \emph{d}
and \emph{p} fall within the min/max range of the multiverse's
(unrounded) recalculated values.
\item \code{d_results}: Data frame detailing all calculated effect sizes and
CIs. Columns include \code{sd_interpretation} (\code{"sd"} or
\code{"se_converted"}), \code{d_unrounded}, \code{ci_lower_unrounded},
and \code{ci_upper_unrounded}. No output rounding is applied; format
in the reporting layer if needed.
\item \code{p_results}: Data frame detailing all calculated p-values.
Columns include \code{sd_interpretation} and \code{p_unrounded}.
}
}
\description{
This function explores the "multiverse" of possible Cohen's \emph{d} effect sizes,
confidence intervals (CIs), and \emph{p}-values that are compatible with a set
of reported summary statistics (Means, SDs, Ns).

The multiverse is generated by varying:
\itemize{
\item \strong{Input Rounding/Grid generation}: Adjusting reported statistics
(Means and SDs) by creating a grid of potential "true" values within
the precision limits defined by \code{m_digits} and \code{sd_digits}.
This relies on the \code{rounding} argument to determine how reported
values are mapped back to their preimage interval (see
\code{recalc_rounding} for the full taxonomy).
\item \strong{Effect Size Type}: Computing both Cohen's \emph{d} and Hedges' \emph{g}
(controlled by \code{hedges_correction}).
\item \strong{CI Methods}: Calculating Wald-type CIs (using \emph{t} distributions
with pooled or Welch variance) and Noncentral \emph{t} (NCT) distribution CIs.
\item \strong{P-value Methods}: Using \emph{t}-tests (Student's pooled or Welch's
unpooled).
\item \strong{Direction}: The calculation uses the direction implied by the
order of the supplied means (M1 - M2) or its reverse, depending on
\code{direction}.
\item \strong{SD vs. SE Interpretation (optional)}: If
\code{include_se_sd_confusion = TRUE}, the function includes a branch
where reported SDs are interpreted as standard errors and converted to
SDs via \eqn{SD = SE \times \sqrt{n}}.
}
}
\examples{
# --------------------------------------------------------------------------
# Example: Checking reported stats from a paper
# --------------------------------------------------------------------------
# Scenario: A paper reports:
#   Group 1: M = 10.30, SD = 3.12, N = 50
#   Group 2: M = 8.71,  SD = 2.80, N = 48
#   Results: t-test p < .01, Cohen's d = 0.53, 95\% CI [0.20, 0.90]

res <- independent_t_test_summary(
  # --- Essential Arguments ---
  m1 = 10.30,
  m2 = 8.71,
  sd1 = 3.12,
  sd2 = 2.80,
  n1 = 50,
  n2 = 48,
  # Define the precision of the input Means and SDs (decimal places)
  m_digits = 2,
  sd_digits = 2,

  # --- Comparison Targets ---
  # Reported p-value is < .01
  p = 0.01,
  p_digits = 2,
  p_operator = "less_than",

  # Reported Effect Size (Cohen's d) and CIs
  d = 0.53,
  d_ci_lower = 0.20,
  d_ci_upper = 0.90,
  d_digits = 2,

  # --- Multiverse Configuration ---
  # Method settings (NULL = check all variants)
  p_methods = NULL,         # Check both Student's and Welch's
  ci_methods = NULL,        # Check Wald (Student/Welch) and NCT
  rounding = "either",      # Assume reported values came from half-up
                            # or bankers rounding (use "truncate" for
                            # truncated reports)
  hedges_correction = NULL, # Check both d and g

  # Assumptions
  alpha = 0.05,
  direction = "m1_minus_m2",
  include_se_sd_confusion = FALSE
)

# View the summary of reproduction
res$reproduced

# Plots
plot_multiverse_p(res)
plot_multiverse_d(res)

}
