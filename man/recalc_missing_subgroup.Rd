% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/subgroups.R
\name{recalc_missing_subgroup}
\alias{recalc_missing_subgroup}
\title{Derive the implied N, mean, and SD of an unreported subgroup}
\usage{
recalc_missing_subgroup(
  reported_ns,
  reported_means,
  reported_sds = NULL,
  overall_n,
  overall_mean,
  overall_sd = NULL,
  scale_min = NULL,
  scale_max = NULL,
  n_digits = NULL,
  mean_digits = NULL,
  sd_digits = NULL,
  rounding = "either"
)
}
\arguments{
\item{reported_ns, reported_means}{Numeric vectors. Statistics for the
\eqn{k - 1} reported subgroups.}

\item{reported_sds}{Numeric vector (optional).}

\item{overall_n, overall_mean, overall_sd}{Numeric. Reported overall values.
\code{overall_sd} is required to derive the SD row.}

\item{scale_min, scale_max}{Numeric. Optional logical endpoints of the
measurement scale (e.g. \code{1} and \code{7} for a 7-point Likert). When supplied,
the \code{M_miss} row is checked against \verb{[scale_min, scale_max]} and the
\code{SD_miss} row is additionally checked against the Bhatia-Davis bound. Both
must be supplied together.}

\item{n_digits, mean_digits, sd_digits}{Integer. Number of decimal places each
value was reported to. No defaults: the caller must specify the precision
of every value they supply. \code{sd_digits} is required only when
\code{reported_sds} and \code{overall_sd} are provided.}
}
\value{
Tibble with one row per derived quantity. The \code{consistent} column
reports the feasibility verdicts described in Details; \code{reported}
columns remain \code{NA} (there is no reported missing-group value).
}
\description{
Given the overall N, M, and (optionally) SD and the reported subgroups,
invert the aggregation identities used by \code{\link[=recalc_total_from_subgroups]{recalc_total_from_subgroups()}}
to recover what an unreported \eqn{k}-th subgroup would have to be for the
reported totals to be consistent.
}
\details{
\deqn{n_\star = N - \sum_g n_g}
\deqn{M_\star = (N M - \sum_g n_g M_g) / n_\star}
\deqn{s_\star^2 = \big((N - 1) \text{SD}^2 - \sum_g (n_g - 1) s_g^2
                    - n_\star (M_\star - M)^2 - \sum_g n_g (M_g - M)^2\big)
                 / (n_\star - 1)}

The \code{consistent} column reports physical-feasibility checks on the implied
missing-subgroup statistics:

\describe{
\item{\code{n_miss}}{\code{TRUE} iff the recalculated interval reaches \eqn{\geq 1}
(i.e. some rounding makes the missing group non-empty); \code{FALSE}
if the reported subgroup Ns already saturate or exceed
\code{overall_n}.}
\item{\code{M_miss}}{When \code{scale_min} and \code{scale_max} are supplied, \code{TRUE}
iff the recalculated interval intersects \verb{[scale_min, scale_max]}.
\code{NA} when scale endpoints are not supplied (no bound to check).}
\item{\code{SD_miss}}{\code{FALSE} if the propagated interval contains \code{NaN} —
i.e. the implied variance is negative at some rounding corner, so
no real subgroup reconciles the reported values. When \code{scale_min}
and \code{scale_max} are supplied, additionally \code{FALSE} if the implied
SD exceeds the maximum Bhatia-Davis bound
\eqn{\sqrt{n_\star/(n_\star - 1)\,(M_\star - a)(b - M_\star)}}
attainable over the rounding box (with \code{a = scale_min},
\code{b = scale_max}). The bound is the largest sample SD physically
attainable by any \eqn{n_\star}-sample on \eqn{[a, b]} with mean
\eqn{M_\star} (Bhatia & Davis, 2000), Bessel-corrected for the
sample variance.}
}
}
\examples{
# 7-point Likert: bound checks via Bhatia-Davis and scale endpoints
recalc_missing_subgroup(
  reported_ns    = 50,
  reported_means = 3.50,
  reported_sds   = 1.40,
  overall_n = 100, overall_mean = 4.50, overall_sd = 1.50,
  scale_min = 1, scale_max = 7,
  n_digits = 0, mean_digits = 2, sd_digits = 2
)
}
\references{
Bhatia, R., & Davis, C. (2000). A better bound on the variance.
\emph{American Mathematical Monthly}, 107(4), 353-357.
}
