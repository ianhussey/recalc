% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/infrastructure.R
\name{.union_summary}
\alias{.union_summary}
\title{Gap-aware union summary from per-method intervals.}
\usage{
.union_summary(method_intervals, value_num, operator, digits)
}
\description{
Merges the per-method rounded intervals (treating a gap of at most one
reporting ULP as contiguous) into disjoint intervals, then reports the
union-based \code{inbounds} decision plus the total covered width, the hull
width, and \code{covered_fraction = covered_width / hull_width} (small means the
hull is mostly gap). Widths are measured at reporting resolution: each
disjoint interval also covers its own ULP cell. For the inequality operators
the union adds no sharpness (a half-line), so the hull rule is used.
}
\keyword{internal}
