% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/regression_from_cor_mat.R
\name{triangle_to_cor_matrix}
\alias{triangle_to_cor_matrix}
\title{Convert the triangle of a correlation matrix to a full symmetric matrix}
\usage{
triangle_to_cor_matrix(triangle)
}
\arguments{
\item{triangle}{A square matrix or data frame. The unused half should
contain \code{NA}.}
}
\value{
A full symmetric correlation matrix, with row and column names
preserved from the input.
}
\description{
Accepts either an upper or a lower triangle (the function auto-detects by
checking which half is all-NA) and returns the full symmetric matrix with
ones on the diagonal. Useful for converting a triangle reported in an
article into an input for \code{recalc_regression_from_cor_mat()}.
}
\examples{
lower <- matrix(NA, 3, 3,
                dimnames = list(c("y", "x1", "x2"),
                                c("y", "x1", "x2")))
lower[lower.tri(lower, diag = TRUE)] <- c(1, 0.5, 0.3, 1, 0.2, 1)
triangle_to_cor_matrix(lower)
}
