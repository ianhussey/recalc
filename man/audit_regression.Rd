% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/audit.R
\name{audit_regression}
\alias{audit_regression}
\title{Run every applicable recalc check on a regression report}
\usage{
audit_regression(
  n = NULL,
  k = NULL,
  r2 = NULL,
  adj_r2 = NULL,
  f = NULL,
  p_model = NULL,
  p_model_op = "eq",
  sd_y = NULL,
  b = NULL,
  se = NULL,
  t = NULL,
  p = NULL,
  p_op = "eq",
  beta = NULL,
  ci_lower = NULL,
  ci_upper = NULL,
  r_y = NULL,
  sd_x = NULL,
  sr2 = NULL,
  predictor_names = NULL,
  r_12 = NULL,
  delta_r2_blocks = NULL,
  level = 0.95,
  diagnostics = TRUE,
  n_digits = 0,
  k_digits = 0,
  r2_digits = 2,
  adj_r2_digits = 2,
  f_digits = 2,
  p_model_digits = 3,
  sd_y_digits = 2,
  b_digits = 2,
  se_digits = 2,
  t_digits = 2,
  p_digits = 3,
  beta_digits = 2,
  ci_digits = 2,
  r_y_digits = 2,
  sd_x_digits = 2,
  sr2_digits = 3,
  r_12_digits = 2,
  delta_r2_blocks_digits = 2,
  rounding = "either"
)
}
\arguments{
\item{n, k}{Numeric scalars.}

\item{r2, adj_r2, f}{Numeric scalars (optional).}

\item{p_model}{Numeric scalar (optional). Reported model p.}

\item{p_model_op}{Comparison op for \code{p_model}.}

\item{sd_y}{Numeric scalar (optional).}

\item{b, se, t, p, beta}{Numeric vectors of length k (optional).}

\item{p_op}{Comparison op for per-predictor p.}

\item{ci_lower, ci_upper}{Numeric vectors of length k (optional).}

\item{r_y, sd_x, sr2}{Numeric vectors of length k (optional).}

\item{predictor_names}{Character vector of length k (optional).}

\item{r_12}{Numeric scalar (optional, k = 2 only).}

\item{delta_r2_blocks}{Numeric vector (optional, B5 only).}

\item{level}{Numeric. CI level. Default 0.95.}

\item{diagnostics}{Logical. If TRUE (default), run E1, E2, E3 diagnostics
where their inputs are available.}

\item{n_digits, k_digits}{Integer.}

\item{r2_digits, adj_r2_digits, f_digits, p_model_digits}{Integer.}

\item{sd_y_digits}{Integer.}

\item{b_digits, se_digits, t_digits, p_digits}{Integer.}

\item{beta_digits, ci_digits}{Integer.}

\item{r_y_digits, sd_x_digits, sr2_digits}{Integer.}

\item{r_12_digits, delta_r2_blocks_digits}{Integer.}

\item{rounding}{See \code{\link{recalc_rounding}} for the accepted values.}
}
\value{
Tibble of every applicable check, with an extra \code{predictor}
column (\code{"model"} or a predictor name/index).
}
\description{
Takes the full set of quantities a paper might report and dispatches every
check whose inputs are all available. Per-predictor checks (A1-A4, A6 bound,
A8) run once per predictor whose inputs are present; model-level (B1-B5) and
cross-equation (C1, C2, C4, C6) checks run once when their inputs are
present. Missing inputs cause the corresponding checks to be skipped
silently. By default also runs the section-E label-confusion diagnostics
(E1 beta vs b, E2 R^2 vs adj R^2, E3 one- vs two-tailed p) where their
inputs are available.
}
\details{
C5 (hierarchical Delta F) is structurally distinct (requires a nested-model
pair with separate R^2, k, and increment metadata) and is not dispatched by
this wrapper - call \code{recalc_delta_f()} directly.
}
\examples{
audit_regression(
  n = 1923, k = 2, r2 = 0.41,
  beta = c(-0.39, 0.47), r_y = c(-0.53, 0.61),
  sd_y = 1.4, sd_x = c(1.2, 0.5),
  sd_y_digits = 1, sd_x_digits = 1,
  predictor_names = c("AI_reliance", "override_freq")
)
}
