% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/diagnostics.R
\name{diagnose_beta_label}
\alias{diagnose_beta_label}
\title{Diagnose whether a coefficient column is standardized or unstandardized}
\usage{
diagnose_beta_label(
  coef_reported,
  r_y,
  sd_x,
  sd_y,
  r2,
  coef_digits = 2,
  r_y_digits = 2,
  sd_x_digits = 1,
  sd_y_digits = 1,
  r2_digits = 2,
  rounding = "either"
)
}
\arguments{
\item{coef_reported}{Numeric vector. Reported values from the column
labeled "beta" (or "b").}

\item{r_y}{Numeric vector. Zero-order correlations of predictors with Y.}

\item{sd_x}{Numeric vector. Predictor SDs (one per predictor).}

\item{sd_y}{Numeric scalar. Outcome SD.}

\item{r2}{Numeric. Reported model R^2.}

\item{coef_digits, r_y_digits, sd_x_digits, sd_y_digits, r2_digits}{Integer.}
}
\value{
Two-row tibble: one row per interpretation.
}
\description{
Tests two interpretations of a coefficient column reported as "beta":
(A) the values are true betas; (B) the values are unstandardized b's
mislabeled as beta. Returns the recalculated-R^2 interval under each
interpretation so the user can see which (if either) is consistent with
the reported R^2.
}
\details{
If A is consistent and B is not, the labeling is correct. If B is consistent
and A is not, the column is mislabeled. If neither is consistent, the gap
is structural.
}
\examples{
diagnose_beta_label(
  coef_reported = c(-0.39, 0.47),
  r_y = c(-0.53, 0.61),
  sd_x = c(1.2, 0.5),
  sd_y = 1.4,
  r2 = 0.41,
  sd_x_digits = 1, sd_y_digits = 1
)
}
