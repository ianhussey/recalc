% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/recalc_independent_t.R
\name{.ci_nct}
\alias{.ci_nct}
\title{Noncentral-t CI on the standardised mean difference.}
\usage{
.ci_nct(t_obs, df, n1, n2, alpha, J = 1, max_ncp = 1000)
}
\description{
Inverts the noncentral-t distribution at the observed t. Larger ncp
shifts the distribution right, so:
\itemize{
\item The UPPER ncp bound (delta_upper) satisfies
P(T_ncp(df) <= t_obs) = alpha/2  (lower.tail = TRUE).
\item The LOWER ncp bound (delta_lower) satisfies
P(T_ncp(df) >= t_obs) = alpha/2  (lower.tail = FALSE).
Then d_bound = delta_bound * sqrt(1/n1 + 1/n2), and Hedges' J is applied
to both bounds.
}
}
\keyword{internal}
