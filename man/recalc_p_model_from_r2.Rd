% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/compose.R
\name{recalc_p_model_from_r2}
\alias{recalc_p_model_from_r2}
\title{Recover model p from R^2 directly (composition: B1 -> B2)}
\usage{
recalc_p_model_from_r2(
  r2,
  n,
  k,
  p_model = NULL,
  p_op = "eq",
  r2_digits = 2,
  n_digits = 0,
  k_digits = 0,
  p_model_digits = 3,
  rounding = "either"
)
}
\arguments{
\item{r2, n, k}{Numeric.}

\item{p_model}{Numeric. Reported model p (optional).}

\item{p_op}{One of \code{"eq"}, \code{"lt"}, \code{"le"}, \code{"gt"},
\code{"ge"}.}

\item{r2_digits, n_digits, k_digits, p_model_digits}{Integer.}
}
\value{
One-row tibble.
}
\description{
When F is not reported but R^2, N, and k are, model p can be computed by
chaining B1 (F from R^2) into B2 (p from F). This helper runs the chain
as a single propagation so rounding intervals are handled correctly across
both stages.
}
\examples{
recalc_p_model_from_r2(r2 = 0.41, n = 1923, k = 2,
                       p_model = 0.001, p_op = "lt")
}
